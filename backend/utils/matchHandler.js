const sessionStore = {};

const matchHandler = () => {
    launchMatchmaking();

    // have a global store to store location ID's and associated open matchmaking queues
    // when launf
};

const launchMatchmaking = (userId, locationId) => {};
